{
    "name": "vg-api-tests",
    "version": "1.0.0",
    "description": "Coding Challenge - API Tests for Vanguard",
    "author": "Marc Dacanay",
    "scripts": {
        "mocha": "node ./node_modules/mocha/bin/mocha",
        "test-dev": "cross-env NODE_ENV=dev mocha --recursive --timeout 30000 --reporter mochawesome",
        "test-int": "cross-env NODE_ENV=int mocha --recursive --timeout 30000 --reporter mochawesome"
    },
    "dependencies": {
        "chai": "^4.2.0",
        "chai-as-promised": "^7.1.1",
        "chai-json-schema": "^1.5.1",
        "mocha": "^7.1.0",
        "mochawesome": "^5.0.0",
        "superagent": "^5.2.2",
        "superagent-jsonp": "^0.2.1"
    },
    "devDependencies": {
        "cross-env": "^7.0.2"
    }
}
